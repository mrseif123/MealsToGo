import React from "react";
import { CompactInfo } from "../../../components/restaurants/compact-info.component";

export const RestaurantCallout = ({ restaurant }) => {
  return <CompactInfo restaurant={restaurant} />;
};
